{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm run build --filter client",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/((?!assets/|api/).*)", "destination": "/index.html" }
  ]
}
