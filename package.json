{
  "name": "prima-facie",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "dev": "concurrently \"pnpm --filter server dev\" \"pnpm --filter client dev\"",
    "build": "pnpm --filter client build",
    "lint": "oxlint server/src client/src --ts --disable-all --rules",
    "lint:fix": "oxlint server/src client/src --ts --disable-all --rules --fix"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "concurrently": "^8.2.2",
    "oxlint": "^1.83.0",
    "playwright": "^1.63.0",
    "typescript": "^5.9.3"
  }
}
